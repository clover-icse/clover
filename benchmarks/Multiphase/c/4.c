int main(){
    int x, y, z;

    // pre-conditions
    x = 0;
    y = 0;
    z = 0;

    // loop body
    while(unknown()){
        if(y > x){
            z = z + 1;
        }
        y = y + x;
        x = x + 1;
        
    }

    // post-condition
    if(x >= 3452365){
        assert(z > 0);
    }
    
}