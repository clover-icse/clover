int main(){
    int x, y, z, w;

    // pre-conditions
    x = 52;
    y = 97;
    z = -76;
    w = 0;

    // loop body
    while(unknown()){
        z = (-5) * x + 3 * y + 4 * z - 8754;
        if(z > 0){
            w = w - x;
        }
        x = 13 - 7 * x;
        y = 54 - 2 * y;
    }

    // post-condition
    if(y >= 80914){
        assert(w > 0);
    }
    
}